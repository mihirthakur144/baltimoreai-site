name = "baltimoreai-demos"
main = "src/worker.js"
compatibility_date = "2025-01-15"

# ─── Layer 1: Per-IP rate limit ────────────────────────────────
# 8 requests per 60 seconds per IP. namespace_id is an arbitrary integer
# (just keep it stable across deploys).
[[unsafe.bindings]]
name = "RATE_LIMITER"
type = "ratelimit"
namespace_id = "1001"
simple = { limit = 8, period = 60 }

# ─── Layer 2: Daily global cap (KV-backed counter) ─────────────
# KV namespace "DEMO_LIMITS_civic" already created (via the Cloudflare
# connector) and its id is filled in below. What's left, from inside the
# worker/ directory:
#
#   wrangler secret put ANTHROPIC_API_KEY
#   wrangler deploy
#
[[kv_namespaces]]
binding = "DEMO_LIMITS"
id = "9b2418c7a56147de8ce7d2cbc8f50321"
